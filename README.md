# ProstaMed V4 Public Code Backup

This repository branch is a public code backup of the ProstaMed / ProstaNet clinical platform.

It includes application code, clinical logic, tests, templates, static assets, workflows, and public manifests.
It intentionally excludes local private data, PHI, database files, audio captures, environment files, local virtual environments, logs, and generated runtime outputs.

For a complete local byte-for-byte operational backup including private files and local databases, use the local archive:
`/Users/oscaralvarado/Desktop/Prostamed V4.zip`.
