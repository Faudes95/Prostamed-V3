"""
ProstaNet AI — Deep Learning and Intelligence Layer.

Provides neural network models, tokenization, training pipelines,
inference services, and clinical explanation generation for
real-time therapeutic recalculation.

Modules:
    tokenizer  — Converts longitudinal patient records into temporal token sequences
    models     — Neural network architectures (Transformer, DeepSurv, VAE, NLP)
    training   — Data loaders, trainers, synthetic data generation, evaluation
    inference  — Unified prediction service, model registry, confidence scoring
    explanations — SHAP-based explanations, clinical narrative generation
"""
